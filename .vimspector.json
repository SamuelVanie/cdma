{
  "configurations":{
    "run":{
      "default": true,
      "adapter": "debugpy",
      "configuration": {
        "request": "launch",
        "program": "${workspaceRoot}/cdma.py",
        "cwd": "${workspaceRoot}"
      }
    }
  }
}
